/*
 * This file is generated by jOOQ.
 */
package nl.tudelft.simulation.lcms.data.tables;


import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import nl.tudelft.simulation.lcms.data.Indexes;
import nl.tudelft.simulation.lcms.data.Keys;
import nl.tudelft.simulation.lcms.data.Lcms;
import nl.tudelft.simulation.lcms.data.tables.Activiteit.ActiviteitPath;
import nl.tudelft.simulation.lcms.data.tables.records.BijlageRecord;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableLike;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class Bijlage extends TableImpl<BijlageRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>lcms.bijlage</code>
     */
    public static final Bijlage BIJLAGE = new Bijlage();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<BijlageRecord> getRecordType() {
        return BijlageRecord.class;
    }

    /**
     * The column <code>lcms.bijlage.id</code>.
     */
    public final TableField<BijlageRecord, Integer> ID = createField(DSL.name("id"), SQLDataType.INTEGER.nullable(false).generatedByDefaultAsIdentity(), this, "");

    /**
     * The column <code>lcms.bijlage.activiteit_id</code>.
     */
    public final TableField<BijlageRecord, Integer> ACTIVITEIT_ID = createField(DSL.name("activiteit_id"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>lcms.bijlage.type</code>.
     */
    public final TableField<BijlageRecord, String> TYPE = createField(DSL.name("type"), SQLDataType.VARCHAR(45).nullable(false), this, "");

    /**
     * The column <code>lcms.bijlage.inhoud</code>.
     */
    public final TableField<BijlageRecord, byte[]> INHOUD = createField(DSL.name("inhoud"), SQLDataType.BLOB(65535).nullable(false), this, "");

    private Bijlage(Name alias, Table<BijlageRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private Bijlage(Name alias, Table<BijlageRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>lcms.bijlage</code> table reference
     */
    public Bijlage(String alias) {
        this(DSL.name(alias), BIJLAGE);
    }

    /**
     * Create an aliased <code>lcms.bijlage</code> table reference
     */
    public Bijlage(Name alias) {
        this(alias, BIJLAGE);
    }

    /**
     * Create a <code>lcms.bijlage</code> table reference
     */
    public Bijlage() {
        this(DSL.name("bijlage"), null);
    }

    public <O extends Record> Bijlage(Table<O> path, ForeignKey<O, BijlageRecord> childPath, InverseForeignKey<O, BijlageRecord> parentPath) {
        super(path, childPath, parentPath, BIJLAGE);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class BijlagePath extends Bijlage implements Path<BijlageRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> BijlagePath(Table<O> path, ForeignKey<O, BijlageRecord> childPath, InverseForeignKey<O, BijlageRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private BijlagePath(Name alias, Table<BijlageRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public BijlagePath as(String alias) {
            return new BijlagePath(DSL.name(alias), this);
        }

        @Override
        public BijlagePath as(Name alias) {
            return new BijlagePath(alias, this);
        }

        @Override
        public BijlagePath as(Table<?> alias) {
            return new BijlagePath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Lcms.LCMS;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.BIJLAGE_FK_BIJLAGE_ACTIVITEIT1_IDX);
    }

    @Override
    public Identity<BijlageRecord, Integer> getIdentity() {
        return (Identity<BijlageRecord, Integer>) super.getIdentity();
    }

    @Override
    public UniqueKey<BijlageRecord> getPrimaryKey() {
        return Keys.KEY_BIJLAGE_PRIMARY;
    }

    @Override
    public List<UniqueKey<BijlageRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.KEY_BIJLAGE_ID_UNIQUE);
    }

    @Override
    public List<ForeignKey<BijlageRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_BIJLAGE_ACTIVITEIT1);
    }

    private transient ActiviteitPath _activiteit;

    /**
     * Get the implicit join path to the <code>lcms.activiteit</code> table.
     */
    public ActiviteitPath activiteit() {
        if (_activiteit == null)
            _activiteit = new ActiviteitPath(this, Keys.FK_BIJLAGE_ACTIVITEIT1, null);

        return _activiteit;
    }

    @Override
    public Bijlage as(String alias) {
        return new Bijlage(DSL.name(alias), this);
    }

    @Override
    public Bijlage as(Name alias) {
        return new Bijlage(alias, this);
    }

    @Override
    public Bijlage as(Table<?> alias) {
        return new Bijlage(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public Bijlage rename(String name) {
        return new Bijlage(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public Bijlage rename(Name name) {
        return new Bijlage(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public Bijlage rename(Table<?> name) {
        return new Bijlage(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage where(Condition condition) {
        return new Bijlage(getQualifiedName(), aliased() ? this : null, null, Internal.condition(this, condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Bijlage where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Bijlage where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Bijlage where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Bijlage where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage whereExists(TableLike<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Bijlage whereNotExists(TableLike<?> select) {
        return where(DSL.notExists(select));
    }
}
